export interface menuListProps {
  name: string;
  prefix: React.ReactNode;
  link: string;
}

export interface menuListIconProps {
  name: string;
  prefix: React.ReactNode;
}

export interface subMenuProps {
  name: string;
  link: string;
}
